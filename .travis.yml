language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t  giorgi.katcharava/docker_react -f Dockerfile.dev .

script:
 - docker run -e CI=true giorgi.katcharava/docker_react npm run test